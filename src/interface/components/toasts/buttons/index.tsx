import GenericToastButton from './GenericToastButton';
import AcceptButton from './AcceptButton';
import RefuseButton from './RefuseButton';

export const toastButtons = {
  generic: GenericToastButton,
  accept: AcceptButton,
  refuse: RefuseButton,
};
